{
  "name": "missive-proxy",
  "version": "1.0.0",
  "description": "Proxy HTTP restreint vers l'API Missive (le jeton reste côté serveur).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
